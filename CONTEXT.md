# 频道运营数据

本上下文定义 YouTube 频道运营数据产品中用于接入、保存和分析频道表现的业务语言。
本文件只定义领域术语；默认值、命令、实现状态和验收条件由对应规格、架构文档与代码维护。

## 语言

**YouTube Studio 运营数据**：与一个 YouTube 频道的内容、表现和运营状态相关，且经频道管理者明确授权后可访问的数据集合。首期边界是官方接口可授权读取的数据。
_避免使用_：Studio 所有数据、私有数据

**频道接入**：一个独立的数据接入单元，对应一个具体的 YouTube 频道及其管理者授予的访问权。
_避免使用_：账号接入、Google 账号接入

**运营数据仓库**：为后续查询、比较和分析而持续保存的频道运营数据集合。
_避免使用_：运营看板数据、临时报表

**只读运营操作**：只获取、保存、检索或分析频道运营数据，而不改变 YouTube 频道状态的操作。
_避免使用_：频道管理、运营写入

**源站刷新**：在查询前由用户明确发起的同步，以获取 YouTube 当时可提供的最新数据。
_避免使用_：实时数据、自动刷新

**数据截至时间**：一项查询结果所覆盖的最新数据时间或时间段终点。
_避免使用_：实时性、当前时间

**首次回填窗口**：频道接入建立时从历史数据中初始化运营数据仓库的时间范围。
_避免使用_：数据保留期、同步周期

**分析配置档案**：一个具名的频道数据分析口径集合，包含时间范围、指标、维度和筛选。
_避免使用_：自由配置、任意查询

**事实查询**：通过稳定接口返回可核验频道数据及其数据截至时间的只读查询。
_避免使用_：AI 结论、主观分析

**覆盖矩阵**：记录官方数据能力、数据类别、可用维度、授权或资格限制以及当前支持状态的清单。
_避免使用_：接口列表、全部数据保证

**同步范围**：一次数据同步所包含的数据类别、维度和时间范围，可由持久配置或本次命令的临时参数确定。
_避免使用_：下载全部数据、固定报表

**同步任务**：针对一个同步范围执行数据获取、保存和状态记录的过程。
_避免使用_：后台脚本、批量下载

**断点续传**：同步任务中断后，从已确认的最后位置继续获取未完成范围的行为。
_避免使用_：重新同步、重复下载

**原始证据**：由官方数据源返回的原始结果及其请求条件、获取时间和来源标识，用于复查规范化数据和事实查询。
_避免使用_：日志、缓存

**运营数据工具**：提供频道数据同步、事实查询和证据查看能力的一组只读工具。
_避免使用_：AI 分析器、自动结论

**覆盖状态**：覆盖矩阵对某项官方数据能力当前可用程度和限制的明确标记。
_避免使用_：成功或失败、存在或不存在

**最后可用数据**：在最新一次成功获取中可供查询的数据，不代表它已经追上 YouTube 当前状态。
_避免使用_：实时数据、最新数据

**强制最新查询**：要求本次查询必须完成源站刷新，否则返回失败而不使用最后可用数据的查询。
_避免使用_：无条件实时查询

**频道接入初始化**：从 OAuth 配置和频道选择开始，建立一个频道接入并创建其首次回填同步任务的过程。
_避免使用_：账号登录、自动接入

**OAuth 配置指导**：帮助用户准备 Google Cloud、OAuth 同意屏幕、客户端信息和本地环境配置的操作指引。
_避免使用_：代用户授权、代用户保存密钥

**首条链路数据基线**：建立频道接入时首先同步的频道、视频、上传播放列表元数据，以及频道和视频级表现与互动数据。
_避免使用_：全部 Studio 数据、完整维度预取

**高维细分数据**：按流量来源、设备、地域、受众或收入等维度细分的运营数据，按分析配置档案按需获取。
_避免使用_：核心同步数据、默认全量下载

**原始证据保留期**：原始证据在本机仓库中可供复查的最长时间范围。
_避免使用_：规范化数据保留期、自动永久保留

**受保护凭据**：由操作系统凭据机制保存的 OAuth 令牌或客户端秘密，以及 cookie 文件内容，绝不属于配置、日志、事实查询或 Git 内容。cookie 文件在配置中只允许保存本机路径（ADR 0001）。
_避免使用_：配置值、可导出密钥
